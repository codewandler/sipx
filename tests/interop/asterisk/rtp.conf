; A small, fixed RTP range. The container runs on the host network, so this is the range the
; tests see; keeping it narrow makes a stray listener obvious rather than mysterious.

[general]
rtpstart=12000
rtpend=12100
