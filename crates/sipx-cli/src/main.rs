//! `sipx` — a command line SIP softphone.
//!
//! Scriptable by design: every action reports machine-readable results so calls can be
//! placed, answered and asserted on from a shell or a test suite.

fn main() {
    println!("sipx: not implemented yet — see docs/stories/ for the roadmap");
}
