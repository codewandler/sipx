# sipx 1.0.0-beta.1 — unpublished candidate

This immutable annotated tag was a release candidate, not a published release. Its protected
workflow validated the exact tag and approved Cargo credential, then stopped at the mandatory
provenance gate because the externally configured denylist was not passed to that gate step.

No package rehearsal, crates.io publication, exact registry consumer, installed-CLI proof, Pages
proof, or GitHub Release creation ran after the failure. No `1.0.0-beta.1` sipx package exists on
crates.io, so there is nothing to yank. The tag is retained unchanged as an audit record.

The corrected candidate is `1.0.0-beta.2`. Its reviewed release record is
[`1.0.0-beta.2.md`](1.0.0-beta.2.md).
