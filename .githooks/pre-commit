#!/usr/bin/env bash
# Refuse commits that reference prior-art projects. Install with:
#     git config core.hooksPath .githooks
set -euo pipefail
exec "$(git rev-parse --show-toplevel)/scripts/check-provenance.sh"
